apiVersion: v1
kind: Service
metadata:
  name: {{ include "release-flow-test.fullname" . }}
  labels:
    {{- include "release-flow-test.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "release-flow-test.selectorLabels" . | nindent 4 }}
